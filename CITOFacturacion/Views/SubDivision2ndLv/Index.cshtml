@model IEnumerable<CITOFacturacion.Models.Entities.SubDivision2ndLv>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShortName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LongName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Abbreviation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OriginCityId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DestinationCityId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShortName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LongName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Abbreviation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OriginCityId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DestinationCityId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubDivision2ndLvId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubDivision2ndLvId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubDivision2ndLvId })
        </td>
    </tr>
}

</table>
